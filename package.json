{
  "name": "efsosotomasyon",
  "version": "1.2.21",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "release:patch": "npm test && npm version patch --no-git-tag-version && npm run build",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
